
# Multiplicação de Matrizes
def matmul(A, B):
    linhaA = len(A)
    colunaA = len(A[0])

    linhaB = len(B)
    colunaB = len(B[0])

    if colunaA == linhaB:

    else:
        print("colA != rowB")
        return None
